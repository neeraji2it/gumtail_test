<li>
	<%= question.body %> by <%= question.user.full_name %>
	<% if question.answered %>
		<p> Answer: <%= question.answer.body %> </p>
	<% elsif current_user.id != question.user_id %>
		<%= render partial: 'answers/new', locals: { question: question } %>
	<% end %>

</li>