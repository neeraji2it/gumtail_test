<%= form_for(current_user.relationships.find_by_publisher_id(@user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unsubscribe", class: "btn btn-red btn-large" %>
<% end %>