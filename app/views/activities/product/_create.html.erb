<% if product %>
  Listed a new product <%= link_to product.product_title, product_url(:subdomain =>product.store.store_name,:number => product.random_no) %>
<% else %>
  added a product which has since been removed
<% end %>